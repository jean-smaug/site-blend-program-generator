
const types = [{ key: 'tech', name: 'Techo' },
  { key: 'blend', name: 'Blend' },
  { key: 'market', name: 'Marketing' },
  { key: 'design', name: 'Design' }];

export default function typeConferences(state = types, action) {
  switch (action.type) {
    default:
      return state;
  }
}
